import React from "react";
import './style.css';

const OrganizationPasses = (props) => {
    console.log(props);
    return (
        <div>Organization Passes</div>
    );
};

export default OrganizationPasses;